name: ci-app
jobs:
  build: # имя взято для примера
	runs-on: ubuntu-latest

	steps:
	  -name: Setup project
	  run: make setup

	  -name: Run tests
	  run: make test

	  -name: Run linter
	  run: make lint
